[plugin.yesoreyeram-infinity-datasource]
# Allow backend (server-side alerting) queries to the Intellidog API.
# Without this the Infinity plugin refuses to proxy requests from the
# Grafana alerting evaluator to non-localhost URLs.
allowed_hosts = http://intellidog:8000
